// Lookup-Map für vollständig ausgebaute Länder (status: 'active' in countries.js).
// Neues Land: hier importieren und unter seiner `id` eintragen.
import morocco from './morocco.js';
import senegal from './senegal.js';
import ethiopia from './ethiopia.js';

export const countryRegistry = {
  morocco,
  senegal,
  ethiopia,
};
